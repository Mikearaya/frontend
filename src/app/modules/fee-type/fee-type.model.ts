   export class IFeeType {
    constructor(
        Id: number,
        Fee: number | string,
        Term: number | string,
        Duration: string | number,
        Min_duration: string | number
    ) {
    }
}
