export class IEvent {
    constructor(
        Id: number,
        Event_name: number | string,
        Description: number | string,
        Start_date: string | number,
        End_date: string | number,
        Start_time: string | number,
        End_time: string | number
    ) {
    }
}
