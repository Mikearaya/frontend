export class IEnrollment {
    constructor(
         id: number,
         student_id: number | string,
         Date: number | string,
         Term: string | number,
         Year: string | number,
         Course: string
          ) {
    }
}
